## Gin's Online Dart's Scoring System v0.2.1

Consolidated release. Replaces v1.2.1.

### Highlights
- Pre-configured Google Client ID + superadmin email baked into `index.html`
- Settings screen always shows sign-in for all users
- Admin tools (Client ID setup) gated to superadmin
- Header sign-in / sign-out icons (Font Awesome SVG)
- Cleanup footer (drop IndexedDB / Esc mentions)
- Router fallback for unknown routes