## Gin's Online Dart's Scoring System v0.2.2

Consolidated release. Replaces v1.2.2, v1.2.3, v1.2.4, v1.2.5, v1.2.6, v1.2.7, v1.2.8.

### Highlights
- Header sign-in icon triggers Google OAuth directly
- Competitions shows email; drop duplicate sign-out button
- Better sign-in feedback for "+ New competition" gate
- Consolidate 3 sign-in call sites into `handleGoogleSignIn()`
- Fix `router is not defined` ReferenceError
- Modal action handler case-sensitivity (`onClick` → `onclick`)
- 1024x600 numpad fix (touch target sizes)

All UI polish and fixups between v0.2.1 and v0.2.2, consolidated.